name: flutter_dev_toolkit_example
description: Example app for Flutter Dev Toolkit
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  shared_preferences: ^2.2.0
  flutter_dev_toolkit:
    path: ../

dev_dependencies:
  flutter_lints:

flutter:
  uses-material-design: true
